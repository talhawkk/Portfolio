/**
 * About page interactions.
 * Skills filtering logic removed as we migrated to a grouped row layout.
 */
document.addEventListener('DOMContentLoaded', () => {
    // Other about page specific scripts can go here in the future.
});
